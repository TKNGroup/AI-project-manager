[project]
name = "aipm-text-data-worker"
version = "0.0.0"
description = ""
requires-python = ">=3.14"
dependencies = [
    "nats-py>=2.7.0",
    "pydantic>=2.0.0",
    "openai>=1.0.0",
    "asyncpg>=0.29.0",
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["main"]
