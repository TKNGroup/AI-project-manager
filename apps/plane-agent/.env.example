LOGGER_LEVEL=trace
HOST=0.0.0.0
PORT=3003
NATS_URL=nats://localhost:4222
# Local self-hosted Plane (run via `make plane_docker_up`)
PLANE_BASE_URL=http://localhost:8090
PLANE_API_KEY=change-me
# Workspace slug from Plane URL: /{workspace-slug}/...
PLANE_WORKSPACE_SLUG=change-me
# Project UUID from project settings or API
PLANE_PROJECT_ID=change-me
